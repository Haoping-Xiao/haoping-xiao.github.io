---
title: "Model 决定知道什么，Effort 决定做多少"
description: "理解 Claude Code 里 model 和 effort 的区别：一个影响能力边界，一个影响 agent 愿意做多少验证。"
pubDate: 2026-07-09
techDomain: ["LLM"]
product: ["Claude"]
---

我以前不喜欢 Claude Code 里的 `thinking effort`。我以为它只是控制「想多久」：reasoning 更长、token 更多，但不一定更聪明。

看完 Claude Devs 的那条 X 推文之后，我意识到这个理解太窄了。**Effort 控制的不是一段 reasoning 的长度，而是整个 agent loop 的工作强度。** 它影响 Claude 在回答前愿意做多少事：读多少文件、跑不跑验证、会不会多查一层、遇到不确定时是继续推进还是回来问你。

## Model 和 Effort 不是同一个旋钮

Model 和 effort 看起来都能让结果变好，但它们解决的问题不同。

| 设置 | 改变什么 | 适合解决的问题 |
|------|----------|----------------|
| Model | Claude 已经具备的能力和知识边界 | 问题太难、领域太陌生、判断质量不够 |
| Effort | Claude 为这次任务投入多少工作 | 没读全文件、没验证、太早停下 |

Model 更像是「这个人本来懂多少」。Effort 更像是「这个人这次愿意认真做到什么程度」。一个强模型低 effort，可能只快速扫一眼；一个普通模型高 effort，也可能通过读上下文、跑测试、反复核对，把明确任务做好。

所以这两个设置不是简单的「越高越好」，而是在回答两个问题：

1. Claude 是不是不知道？
2. Claude 是不是没认真做够？

## 先看 Context，再调设置

当 Claude 给出一个坏结果时，我现在不想第一时间换模型，也不想第一时间拉高 effort。

更应该先看的是 context：

- prompt 有没有说清楚目标？
- 相关文件有没有放进上下文？
- Claude 有没有可用的工具和 skills？
- 任务有没有明确的验收方式？

如果这些东西没有给到，调 model 或 effort 都像是在补一个上游问题。比如你让 Claude「优化这个页面」，但没说用户是谁、优化什么指标、设计风格是什么、怎么验收，它输出不满意，不一定是模型或 effort 的问题，更可能是工作现场不清楚。

好的 context 会把任务从「猜」变成「做」。先把 context 补齐，再判断要不要动设置。

<details>
<summary>技术实现</summary>

这里可以补一个实现层面的直觉：很多所谓「是否 thinking」的开关，并不一定是神秘的推理按钮，而是通过 **chat template 控制输入格式** 来实现的。

先看 Qwen3。`enable_thinking=True` 时，模板基本上只是把对话拼到 assistant 的生成起点：

```text
<|im_start|>assistant
```

它不会提前关闭 thinking，而是让模型自己继续生成。因为模型在训练时学过 Qwen3 的回答格式，所以接下来往往会自己进入推理块：

```text
<think>
这里是推理过程……
</think>

这里是最终回答。
```

而 `enable_thinking=False` 时，模板会直接在 assistant 开头放一个空的 thinking block，等于告诉模型「思考阶段已经结束，直接回答」：

```text
<think>

</think>
```

所以两种模式的差别不是模型权重变了，而是输入格式变了：开启 thinking，是不提前关闭 thinking；关闭 thinking，是模板先替模型关掉 thinking。

GLM-5.2 的开源模板把这件事展示得更直接。在 Hugging Face 的 [`chat_template.jinja`](https://huggingface.co/zai-org/GLM-5.2/blob/main/chat_template.jinja) 里，它不只控制 `<think>`，还会把 `reasoning_effort` 写进系统提示：

```text
<|system|>Reasoning Effort: High
```

如果没有传入 `high`，模板默认使用 `Max`。assistant 开头也会根据 `enable_thinking` 选择插入 `<think>` 或 `<think></think>`：

```text
<|assistant|><think>
```

```text
<|assistant|><think></think>
```

也就是说，至少在 GLM-5.2 这个开源模型里，`thinking effort` 本质上就是一个被模型训练过的文本控制信号，再配合 `<think>...</think>` 格式、生成 token 预算，以及推理框架对 `reasoning_content` 的解析来工作。

可以把这类能力拆成两层：

| 层级 | 控制什么 |
|------|----------|
| Think / No Think | 是否显式进入推理格式 |
| Thinking Effort | 一个更高层的抽象参数，服务端再翻译成模板提示、隐藏指令或控制 token |

关键不在参数名字本身，而在训练。模型必须在训练和后训练阶段学过不同 effort 下该生成多长、做多少自查、何时结束推理，推理时才会稳定响应这些模板信号。

</details>

## 什么时候该换 Model

如果 Claude 已经拿到了足够上下文，也认真尝试了，但仍然在关键判断上错了，那更像是 model 问题。比如：

- subtle bug 看不出来；
- 架构权衡判断不稳；
- 新领域里的概念关系搞混；
- 多次解释后仍然给出看似合理但实际错误的方案。

这时换更强的模型是合理的。问题不在于它有没有多跑一步，而在于它对这个问题的理解能力不够。

## 什么时候该调 Effort

如果 Claude 的错误来自「没做够」，那就是 effort 问题。比如：

- 明明应该读的文件没读；
- 改完代码没有跑测试；
- 只修了表面症状，没有追根因；
- 多步骤任务做到一半就回来总结；
- 没有验证结果，却表现得很确定。

这时候调高 effort 有意义。你需要的不是更强的先验知识，而是更完整的 agent loop：多看、多试、多验证，直到达到足够 confidence 再输出。

## 一个简单判断法

下次 Claude 结果不对，可以按这个顺序问：

| 问题 | 如果答案是「否」 | 该做什么 |
|------|------------------|----------|
| 我给清楚 context 了吗？ | 否 | 先补 prompt、文件、工具、验收标准 |
| Claude 有认真验证吗？ | 否 | 提高 effort 或明确要求验证 |
| Claude 理解这个问题吗？ | 否 | 换更强的 model |

这三个问题的顺序很重要。很多时候，我们以为自己在调模型，其实是在弥补上下文缺失；以为模型不够聪明，其实只是 agent 太早停下了。

## 写在最后

我从那条 X 推文里带走的最重要一句话是：

> Effort changes how much work Claude does. The model changes what Claude knows.

翻成我的理解就是：**Model 决定 Claude 知道什么，Effort 决定 Claude 为这次任务做多少。**

当结果不满意时，先别急着动设置。先检查 context：提示是否清楚，工具是否到位，skills 是否可用，Claude 是否有办法验证自己的工作。然后再问：**它是不知道，还是没做够？**

不知道，是 model 问题；没做够，是 effort 问题。

参考：[Claude Devs on X](https://x.com/ClaudeDevs/status/2074900291062034618)。
